# This "__init__.py" file marks the "config" directory as a Python package.
# It signals Python to import modules from this directory.
# The file is empty, which is perfectly fine for simple packages like in our case.